# Development dependencies

# Note: pandoc **must** be installed to built Jupyter notebook docs. You can
# install pandoc on linux with `sudo apt install pandoc`

docutils
nbsphinx>=0.8.2
recommonmark
sphinx
sphinx_rtd_theme
tox>=3.20.1,<4.0.0
tox-venv>=0.4.0
